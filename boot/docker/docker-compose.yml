version: '3.8'

services:
  postgres:
    image: postgres:14.9
    container_name: guardias-bbdd-container
    environment:
      POSTGRES_PASSWORD: root
      POSTGRES_USER: sa
      POSTGRES_DB: guardias
    ports:
      - "5432:5432"